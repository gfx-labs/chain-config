image: node:20

.yarn-cache:
  before_script:
    - yarn install --immutable
  cache:
    paths:
      - node_modules
      - .yarn
    key:
      files:
        - yarn.lock


build:
  stage: build
  extends: .yarn-cache
  script:
    - yarn build
  artifacts:
    paths:
      - dist

deploy:
  dependencies:
    - build
  stage: deploy
  extends: .yarn-cache
  script:
    - yarn semantic-release
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
